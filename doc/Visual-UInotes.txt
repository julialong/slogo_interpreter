https://hackmd.io/GwVgpmwMxSC0JgGMBMcAswBmU4A490BGOAExQE4UwAjG2m04IA==

Visualizer
	VisualizerMain()	{
		TextBoxViewer
		protected initialize()
	}

	Console() implements VisualizerMain()	{
		scrollable
	}

	Canvas() implements VisualizerMain()	{
	}

	Options() implements VisualizerMain()	{

	}

	Exceptions() implements VisulizerMain()	{
		uses alerts
	}

	Turtle implements VisualizerMain()	{
	}

	Help() implements VisualizerMain()	{
		alert?
		pop-up box
	}

Drawable

Sprint 3:
Jennifer:
* rearrange buttons
* animate turtle
* pause
* reset
* pop-up of images and colors with corresponding numbers
* move turtle graphically

Maya:
* old commands
* old variables
* separate commands (semicolon)
* step

* pop-up of table of all tables with all their info
	* buttons for all properties
HashMap<Drawable, List<String>>
* id
* pen color
* pen thickness
* image path
* xpos
* ypos
* heading
* pen up/down